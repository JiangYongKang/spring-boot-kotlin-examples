// 设置生成的 MANIFEST.MF 文件中的属性，输出文件的位置在 ${root}/build/tmp/bootJar/MANIFEST.MF
bootJar {
    manifest {
        attributes(
                "Manifest-Version": 1.0,
                "Implementation-Version": 1.0,
                "Implementation-Title": "spring boot banner example",
        )
    }
}


