server:
  servlet:
    context-path: /spring-boot-configuring
  undertow:
    max-http-post-size: 0


spring:
  thymeleaf:
    cache: true
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html
    enabled: true

test:
  number: 1

random:
  int: ${random.int}
  less: ${random.int(10)}
  uuid: ${random.uuid}
  secret: ${random.value}